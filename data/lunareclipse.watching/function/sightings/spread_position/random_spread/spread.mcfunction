# List of variables used:
# "distance" - The distance that Herobrine can behind the player.
# "spacing" - The distance Herobrine can be to the left or the right of the player

# Tp the entity executing the command to the position.
$tp @s ^$(spacing) ^ ^$(distance)

# Debug info.
# $say tp @s ^$(spacing) ^ ^$(distance)